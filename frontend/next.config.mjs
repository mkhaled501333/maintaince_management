/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  allowedDevOrigins: [
    'janssencmma.cloud',
    'https://janssencmma.cloud',
    'http://janssencmma.cloud',
  ],
};

export default nextConfig;

